# 需要你补充或确认的内容

# 已确认与待确认内容

## 已确认

- 新项目目录：`D:\Project\MXDScript`。
- 易键鼠类型：单头。
- 允许把 64 位 `msdk.dll` 放到 `vendor/msdk/msdk.dll`，但不提交 Git。
- 游戏窗口大小可能变化，但游戏画面像素图片不会变。
- 旧魔盒 `MoveD` 已确认是带轨迹移动到目标坐标。

## 硬件与 DLL

1. 设备打开方式还需要确认：端口号 `M_Open(1)`、VID/PID，还是 `M_ScanAndOpen()`。
2. VID/PID 还需要确认。你不需要手工查，后续我可以写 `device_probe` 自动读取。

## 游戏环境

1. 冒险岛窗口是否固定为标题 `MapleStory`？
2. 是否有多显示器或 Windows DPI 缩放？
3. 运行时游戏窗口是无边框、窗口化还是全屏窗口化？

## 图像识别

1. 是否继续使用 `E:\MHImg` 作为图片资源根目录？
2. KM 的 `FindPic sim=1.00` 当前先按 `match_threshold = 1.00` 保留；如果 dry-run 日志显示有截图但未命中，可以在 `config/local.toml` 里调低阈值实测。
3. 后续是否需要把每次未匹配时的截图保存到 `captures/` 便于排查？

## 脚本行为

1. `noFindCount >= 1800` 的退出策略是否保留，还是希望改成真实时间超时，例如 9 分钟无推进退出？
2. 如果窗口丢失、设备断开或图片缺失，Python 版是否应该立即退出，还是尝试恢复？

## 日志

1. 当前代码日志先使用英文 key-value 格式，方便搜索和自动分析。
2. 当前已经同时输出到控制台和文件。
3. 当前已经记录匹配坐标、图片路径、动作和退出原因；是否保存未命中截图仍待确认。
